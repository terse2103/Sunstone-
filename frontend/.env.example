# Backend origin. Falls back to http://localhost:8000 when unset (dev default).
# In production this is the deployed FastAPI URL (e.g. https://placementiq-api.onrender.com).
VITE_API_BASE_URL=http://localhost:8000
