# Backend origin. Falls back to http://localhost:8000 when unset (dev default).
# On Vercel the frontend and backend share an origin — leave this empty (or unset)
# so the client emits relative /api/* paths under the same Vercel domain.
VITE_API_BASE_URL=http://localhost:8000
