# Production runtime deps for the Vercel Python function.
# Local development uses backend/requirements.txt (includes uvicorn + dev tools).
fastapi>=0.110,<1.0
pydantic>=2.6,<3.0
anthropic>=0.40,<1.0
python-dotenv>=1.0,<2.0
